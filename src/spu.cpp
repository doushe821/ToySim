#include <iostream>
#include <vector>
#include <memory>
#include <fstream>

#include "architecture.h"

namespace ToySim {
  
  void SPU::Compute(std::vector<int> &Instructions) {
    for (auto &Instruction : Instructions) {
      
    }
  }
} // namespace ToySim
